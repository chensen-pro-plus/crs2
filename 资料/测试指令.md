 已补上限流相关的单元测试，覆盖 rateLimitEndAt 的动态恢复、过期限流清理，以及 RateLimitTracker 的持久化/清理行为。
  具体在 tests/rateLimitTracker.test.js 增加了首次成功清库与持久化重试窗口的断言，在 tests/geminiRateLimit.test.js 增加
  了基于 rateLimitEndAt 的限流判断、过期清理触发，以及选账号时跳过仍受限账户的覆盖。

  未运行测试（本地环境未执行）。

  建议下一步：

  1. 运行局部测试：npm test -- rateLimitTracker.test.js
  2. 运行局部测试：npm test -- geminiRateLimit.test.js