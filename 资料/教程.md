# 🚀 本地运行指南

本文档介绍如何在本地运行 Claude Relay Service 项目。

---

## 📋 前置要求

| 软件 | 版本要求 |
|------|---------|
| **Node.js** | 18 或更高版本 |
| **Redis** | 6 或更高版本 |

---

## 🛠️ 安装步骤

### 第一步：安装依赖

```bash
# 进入项目目录
cd /path/to/claude-relay-service2

# 安装后端依赖
npm install

# 安装前端依赖
npm run install:web
```

### 第二步：配置环境变量

```bash
# 复制配置文件
cp config/config.example.js config/config.js
cp .env.example .env
```

编辑 `.env` 文件，设置以下关键配置：

```bash
# JWT密钥（随便写一个长字符串）
JWT_SECRET=your-super-secret-jwt-key-here

# 加密密钥（必须是32位字符）
ENCRYPTION_KEY=12345678901234567890123456789012

# Redis配置（如果本地Redis无密码，保持默认即可）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
```

### 第三步：确保 Redis 运行

**macOS (Homebrew):**
```bash
# 启动 Redis
brew services start redis

# 检查 Redis 是否运行
redis-cli ping
# 应返回 PONG
```

**Ubuntu/Debian:**
```bash
sudo systemctl start redis-server
redis-cli ping
```

### 第四步：构建前端

```bash
npm run build:web
```

### 第五步：初始化并启动服务

```bash
# 初始化（生成管理员账号密码，保存在 data/init.json）
npm run setup

# 开发模式运行（带热重载）
npm run dev

# 或者后台运行
npm run service:start:daemon
```

### 第六步：访问管理界面

浏览器访问：**http://localhost:3000/web**

管理员账号密码查看：`data/init.json`

---

## 📝 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式运行（带热重载） |
| `npm run service:start:daemon` | 后台运行服务 |
| `npm run service:stop` | 停止服务 |
| `npm run service:restart:daemon` | 重启服务 |
| `npm run service:status` | 查看服务状态 |
| `npm run service:logs` | 查看日志 |
| `npm run service:logs:follow` | 实时查看日志 |

---

## ❓ 常见问题

### Redis 连不上？

```bash
# 检查 Redis 是否启动
redis-cli ping
# 应该返回 PONG

# macOS 重启 Redis
brew services restart redis
```

### 端口被占用？

修改 `.env` 文件中的 `PORT` 配置：
```bash
PORT=3001  # 改成其他端口
```

### 忘记管理员密码？

查看 `data/init.json` 文件获取初始账号密码。

---

## 🔗 相关链接

- 完整文档：[README.md](./README.md)
- 官方网站：https://pincc.ai/
